context("merging and unmerging cells")

# load spreadsheet

# requires a spreadsheet to work
# test_that("Merge Cells", {
#   gs_merge_cells(gap_ss, ws = 1, range = "A1:B2")
#   gs_merge_cells(gap_ss, ws = "Americas", range = "A5:E10", merge_type="MERGE_ROWS")
#   gs_merge_cells(gap_ss, ws = "Americas", range = "A11:C15", merge_type="MERGE_COLUMNS")
# })
# 
# test_that("Get Merged Ranges", {
#   all_merged_ranges <- gs_get_merged_cells(gap_ss)
# })
# 
# test_that("Unmerge Cells", {
#   gs_unmerge_cells(gap_ss, ws = "Americas", range = "A11:C15")
#   gs_unmerge_cells(gap_ss, ws = 1, range = "A1:B2")
# })
# 
# test_that("Unmerge All Cells on Worksheet", {
#   gs_unmerge_cells(gap_ss, ws = 1)
# })
# 
# test_that("Unmerge All Cells in Spreadsheet", {
#   gs_unmerge_cells(gap_ss)
# })
